#include <stdio.h>

typedef int type;

size_t _strlen(const char *str);
char *_strcpy(char *dest, const char *src);
int swap(type * num1, type * num2);
unsigned int do_dup(int a[], int b[],int h[], unsigned int m, unsigned int n);
int is_increase(int array[]);
int max(int n, ...);
